const NumberCalculator = require("../functions/2. Classes/9. numberCalculator.js");

decribe("Number Calculator", () => {
    test("Constructor creates the desired object", () => {
        const nums = new NumberCalculator(1, 2, 3);
    })
})